# import lotto_lib
# from lotto_lib import fn_lotto
from lotto_lib import fn_lotto as f
# 모듈 불러올 때 from으로 함수를 불러오면 앞에 파일명 안붙여도 다이렉트로 사용 가능
print(f(5))

# import math
# print(math.sqrt(5))
from math import *
sqrt(5)
print(factorial(5))